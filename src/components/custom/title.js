import Image from "next/image";
import React from "react";

const Title = () => {
  return (
    <div>
      <Image src="/assets/addUsers.svg" alt="add" width={40} height={40} />
    </div>
  );
};

export default Title;
